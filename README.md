Just do ```python CLI.py``` in the terminal to start the shell 

Then set up your working directory using the ```set_directory <absolte/path/to/wd>``` command and add the ABSOLUTE path to a directory from your computer

The other commands you can see by using the ```help``` command
